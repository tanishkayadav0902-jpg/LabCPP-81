#include<iostream>
namespace SYCS4
{
    int add()
    {
        int a,b,sum;
        std::cout<<"Enter two number";
        std::cin>>a>>b;
        std::cout<<"sum of two numbers is"<<(a+b);
        return 0;
    }
{
    int sub()
    {
        int a,b,sub;
        std::cout<<"Enter two number";
        std::cin>>a>>b;
        std::cout<<"sub of two numbers is"<<(a-b);
        return 0;
    }

}
}